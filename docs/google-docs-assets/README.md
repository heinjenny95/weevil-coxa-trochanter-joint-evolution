# Google Docs image assets

These files are resolution-optimized copies of the corresponding publication figures for embedding in the tabbed Google Docs manuscript. They contain the same plotted data and labels as the full-resolution files in `data/screw_geometry/figures`; only the raster dimensions are reduced to remain below the Google Docs image-size limit.
